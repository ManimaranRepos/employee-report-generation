{
  "name": "employee-report-generation-system",
  "version": "1.0.0",
  "private": true,
  "description": "Modern, responsive Employee Report Generation System (search by EmpID, generate PDF, email it).",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm --workspaces install",
    "seed": "npm --workspace backend run seed",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "start": "npm --workspace backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
